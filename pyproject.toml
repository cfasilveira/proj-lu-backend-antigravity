[project]
name = "proj-lu-backend"
version = "0.1.0"
description = "RH Connect Backend - FastAPI + Supabase"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "supabase",
    "python-multipart",
    "pdfplumber",
    "google-generativeai",
    "pydantic-settings",
    "python-dotenv",
    "bcrypt",
    "httpx"
]
